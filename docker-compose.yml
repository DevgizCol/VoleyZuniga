version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: zuniga_user
  #     POSTGRES_PASSWORD: zuniga_password
  #     POSTGRES_DB: zuniga_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: always

volumes:
  pgdata:
